"""GuIA evaluation harnesses.

A fully additive package (touches no existing app file) that measures how well
GuIA's subsystems work. Run entry points as modules from the repo root, e.g.:

    python -m eval.part1_faithfulness --smoke
    python -m eval.groundtruth --dump

See eval/EVALUATION_PLAN.md for the design and eval/README.md for how to run.
"""
